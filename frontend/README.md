# Vite React Frontend
This is the frontend for the Profile Checker application.

## Prerequisites
Make sure the backend is running first.

## Running locally

```bash
npm install
npm run dev
```

Note: API requests to `/api/*` are proxied to `http://localhost:5000` via `vite.config.js`.
